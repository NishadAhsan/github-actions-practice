name: Build, test and deloy

on:
  push:
    branches:
      - main

      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Dotnet
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '8.0.x'
      
      - name: Check dotnet version
        run: |
          dotnet --info

  test-unit:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Run unit tests
        run: |
          echo "Running unit tests"
          echo "result=All tests passed" >> $GITHUB_ENV

  test-lint:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checking lints
        run: |
          echo "Checking lints"
          echo "result=Lints testing passed" >> $GITHUB_ENV

  notify:
    runs-on: ubuntu-latest
    needs: [test-unit, test-lint]
    if: always()
    steps:
      - name: Notify
        run: |
          echo "Notifying about the build and test results"
          echo "Unit tests ==> ${{ needs.test-unit.result }}"
          echo "Lint ==> ${{ needs.test-lint.result }}" 

